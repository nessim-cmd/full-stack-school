{
  "name": "student-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/student-app",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/student-app && NODE_ENV=production npx next build",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{projectRoot}/.next"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/student-app && npx next dev -p 4002",
        "cwd": "{workspaceRoot}"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/student-app && npx next start -p 4002",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
