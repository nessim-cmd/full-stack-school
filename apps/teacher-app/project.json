{
  "name": "teacher-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/teacher-app",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/teacher-app && NODE_ENV=production npx next build",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{projectRoot}/.next"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/teacher-app && npx next dev -p 4001",
        "cwd": "{workspaceRoot}"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/teacher-app && npx next start -p 4001",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
