{
  "name": "super-admin-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/super-admin-app",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/super-admin-app && NODE_ENV=production npx next build",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{projectRoot}/.next"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/super-admin-app && npx next dev -p 4005",
        "cwd": "{workspaceRoot}"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/super-admin-app && npx next start -p 4005",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
